hl.config({
  input = {
    repeat_delay = 250,
    repeat_rate = 25,
    follow_mouse = 1,

    scroll_method = "2fg",

    touchpad = {
      natural_scroll = true,
      disable_while_typing = true,
      drag_lock = 1,
    },

    touchdevice = {
      enabled = false,
    },
  }
})
